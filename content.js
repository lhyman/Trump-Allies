var elements = document.getElementsByTagName('*');

for (var i = 0; i < elements.length; i++) {
    var element = elements[i];

    for (var j = 0; j < element.childNodes.length; j++) {
        var node = element.childNodes[j];

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Richard Shelby/gi, 'Richard Shelby, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Lisa Murkowski/gi, 'Lisa Murkowski, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Dan Sullivan/gi, 'Dan Sullivan, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Martha McSally/gi, 'Martha McSally, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/John Boozman/gi, 'John Boozman, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Tom Cotton/gi, 'Tom Cotton, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Cory Gardner/gi, 'Cory Gardner, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Marco Rubio/gi, 'Marco Rubio, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Rick Scott/gi, 'Rick Scott, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/David Perdue/gi, 'David Perdue, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Kelly Loeffler/gi, 'Kelly Loeffler, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Mike Crapo/gi, 'Mike Crapo, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Jim Risch/gi, 'Jim Risch, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Todd Young/gi, 'Todd Young, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Mike Braun/gi, 'Mike Braun, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Chuck Grassley/gi, 'Chuck Grassley, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Joni Ernst/gi, 'Joni Ernst, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Pat Roberts/gi, 'Pat Roberts, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Jerry Moran/gi, 'Jerry Moran, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Mitch McConnell/gi, 'Mitch McConnell, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Rand Paul/gi, 'Rand Paul, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Bill Cassidy/gi, 'Bill Cassidy, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/John Kennedy/gi, 'John Kennedy, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Susan Collins/gi, 'Susan Collins, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Roger Wicker/gi, 'Roger Wicker, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Cindy Hyde-Smith/gi, 'Cindy Hyde-Smith, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Roy Blunt/gi, 'Roy Blunt, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Josh Hawley/gi, 'Josh Hawley, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Steve Daines/gi, 'Steve Daines, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Deb Fischer/gi, 'Deb Fischer, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Ben Sasse/gi, 'Ben Sasse, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Richard Burr/gi, 'Richard Burr, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Thom Tillis/gi, 'Thom Tillis, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/John Hoeven/gi, 'John Hoeven, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Kevin Cramer/gi, 'Kevin Cramer, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Rob Portman/gi, 'Rob Portman, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Jim Inhofe/gi, 'Jim Inhofe, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/James Lankford/gi, 'James Lankford, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Pat Toomey/gi, 'Pat Toomey, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Lindsey Graham/gi, 'Lindsey Graham, Trump supporter');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Tim Scott/gi, 'Tim Scott, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/John Thune/gi, 'John Thune, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Mike Rounds/gi, 'Mike Rounds, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Lamar Alexander/gi, 'Lamar Alexander, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Marsha Blackburn/gi, 'Marsha Blackburn, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/John Cornyn/gi, 'John Cornyn, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Ted Cruz/gi, 'Ted Cruz, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Mike Lee/gi, 'Mike Lee, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Mitt Romney/gi, 'Mitt Romney, Trump antagonist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Shelley Moore Capito/gi, 'Shelley Moore Capito, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Ron Johnson/gi, 'Ron Johnson, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/Mike Enzi/gi, 'Mike Enzi, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }

        if (node.nodeType === 3) {
            var text = node.nodeValue;
            var replacedText = text.replace(/John Barrasso/gi, 'John Barrasso, Trump apologist');

            if (replacedText !== text) {
                element.replaceChild(document.createTextNode(replacedText), node);
            }
        }
    }
}